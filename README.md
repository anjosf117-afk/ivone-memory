
# Ivone Prototype (Simple Version)

This is a minimal prototype for deploying Ivone with simple in-memory memory
using Vercel serverless functions.

## Endpoint
POST /api/chat  
Body: { "message": "texto" }

## Notes
- Memory resets on cold start (only for testing).
- Ready to import on Vercel directly.
